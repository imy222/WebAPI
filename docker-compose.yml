version: "3.8"
services:
  unit-tests:
    build: 
      context: .
      dockerfile: ./Dockerfile.buildandtest
      target: test
    
  publish:
    image: docker.myob.com/future-makers-academy/imay-webapi
    build:
      context: .
      dockerfile: ./Dockerfile.buildandtest
      target: app
    ports: 
      - "7149:80"
    
  
  


